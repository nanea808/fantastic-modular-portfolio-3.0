/** @type {import('next').NextConfig} */
const nextConfig = {
//   webpack: (config, options) => {
//     config.module.rules.push({
//       test: /\.(pdf)$/i,
//       use: [{
//         loader: 'file-loader',
//         options: {
//             name: '[name].[ext]'
//         }
//       }],
//     });

//     return config;
//   },
};

module.exports = nextConfig;
